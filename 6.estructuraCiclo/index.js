// Existen 3
// averiguar su concepto aplicacion y ejercicios
//for
 for(let i=2;i<=10;i+=2){
      console.log(i)
    }
    // While
    let eval =true
    let eval2='salir'
    while(eval==true){
      if (eval2='salir'){
                console.log('adios')
          eval=false
        }
      console.log ('hola')
    }
    // -----------------------------------------------------
    // CONCEPTO DE ESTRUCTURA CICLO FOR : 
    // El ciclo for es uno de los más utilizados en programación debido a que permite repetir varias instrucciones (pasos) un cierto número de ocasiones (por ejemplo, 10 veces). 
    // Se emplea en el recorrido de vectores, matrices y estructuras, entre otros.
    // Sobre sus características se puede mencionar que:

// Siempre se hace uso de una variable (contador) que incrementará su valor automáticamente y ayudará a determinar si se continúa o finaliza el ciclo.

// El contador deberá inicializarse con un valor, generalmente 0 ó 1, dependiendo de lo que se esté realizando.

// Un ciclo puede contener otro ciclo dentro de sí (a esto se le denomina ciclo anidado). Nunca se debe utilizar el mismo nombre de la variable (contador) en ambos ciclos, pues el programa no podrá determinar cuándo se finaliza el ciclo.

// ¿Cómo se utiliza el ciclo for?
// El ciclo for en C funciona siguiendo los siguientes pasos:
// Realiza las operaciones de la instrucción uno (si la hay), estas operaciones sólo se ejecutan una vez.
// Evalúa la condición (la instrucción dos)
// Si la condición es verdadera, ejecuta el cuerpo del ciclo for.
// Ejecuta la instrucción tres (si la hay)
// -------------------------------------------------------------
// CONCEPTO DE ESTRUCTURA WHILE 
// Un bucle while tiene asociado un bloque de sentencias que se ejecutarán secuencialmente mientras la condición de entrada al bucle sea cierta.
// La condición se evalúa al comienzo de la estructura.
// Esto supone que el bloque de instrucciones puede no ejecutarse ninguna vez si la condición es inicialmente falsa.

// ---------------------------------------------------------------

// CONCEPTO DE ESTRUCTURA DO-WHILE.

//  Los ciclos do-while tambien son una estructura de control cíclica, los cuales nos permiten ejecutar una o varias líneas de código de forma repetitiva sin necesidad de tener un valor inicial e incluso a veces sin siquiera conocer cuando se va a dar el valor final, hasta aquí son similares a los ciclos while,
// el ciclo do-while nos permite añadir cierta ventaja adicional y esta consiste que nos da la posibilidad de ejecutar primero el bloque de instrucciones antes de evaluar la condición necesaria, de este modo los ciclos do-while, son más efectivos para algunas situaciones especificas.
// En resumen un ciclo do-while, es una estructura de control cíclica que permite ejecutar de manera repetitiva un bloque de instrucciones sin evaluar de forma inmediata una condición especifica,
// sino evaluándola justo después de ejecutar por primera vez el bloque de instrucciones.